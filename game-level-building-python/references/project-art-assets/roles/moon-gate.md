# moon_gate

This is a role-model doc, not a current candidate list.

## Current Status

The cleaned inventory does not yet expose a trustworthy explicit `moon_gate` pool.

That is useful information by itself:

- the generator role exists
- the project naming does not yet make this role machine-readable

## Current Practical Fallback

Use the broader gate family when a moon-gate beat is needed:

- [gate](gate.md)

Best temporary sources:

- `BP_proto_env_L_Gate_02_Actor`
- `BP_proto_env_L_Gate_03_Actor`
- `BP_proto_env_L_Gate_04_Actor`

## Naming Request For Future Assets

All future moon-gate assets should literally contain:

- `MoonGate`

Examples:

- `BP_CourtGarden_MoonGate_Round_Assembled_A01`
- `SM_CourtGarden_MoonGate_Frame_Module_A01`

That one token will make future scans, generators, and palette manifests much more reliable.
